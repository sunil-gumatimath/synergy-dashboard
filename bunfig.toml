# Bun configuration for Aurora EMS
# This project uses Bun exclusively — do not use npm, yarn, or pnpm

[install]
# Save exact versions to bun.lock
exact = false

[install.cache]
# Use a shared cache directory
dir = "~/.bun/install/cache"

[run]
# Use bun as the JS runtime for scripts
bun = true
